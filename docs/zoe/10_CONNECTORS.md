# Zoe CLI — Connectors

v1 coding workflow priorities are local Git inspection and GitHub authentication already used for login. Git write operations, Docker, Supabase, Insforge project management, deployments, issue trackers and documentation tools are deferred until core runtime stability is proven.

Each future connector must define user value, isolated authentication, allowed actions, permission capability, risks, minimum viable workflow and deterministic acceptance criteria. No connector is implemented during stabilization. Status: Pending owner decision on the first external connector after Git integration.
